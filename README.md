# Classwork – Loops in C++

This project contains a C++ program that practices basic loop concepts.

##  Features

The program performs the following tasks:

1. Displays all **even positive numbers** in the interval \[n, m\]  
   - One number per line
2. Calculates and displays the **sum of numbers from 1 to 100**
3. Calculates the **factorial of a given number n**
   - Input is limited to avoid integer overflow

## Requirements

- C++ compiler (g++)
- Linux / macOS / Windows (with g++ installed)


##  How to Compile and Run

```bash
g++ main.cpp -o main (compile)
./main (Run)




